# タスク1: サブエージェントから冗長ルール削除

## 背景

以下2ルールは `.editorconfig` + CI で完全にカバー済みのため、サブエージェントでのチェックは不要。

| ルール | editorconfig 設定 |
|--------|------------------|
| アクセス修飾子 (ルール3) | `dotnet_style_require_accessibility_modifiers = always:warning` |
| var 使用 (ルール6) | `csharp_style_var_* = true:warning` (3設定) |

## 変更対象

- `~/.claude/agents/unity-code-quality-checker.md`

## 変更内容

### 削除する箇所

**ルール3（アクセス修飾子、29-31行目付近）:**
```
3. **アクセス修飾子**:
   - 全ての場所に明示的にアクセス修飾子をつける必要がある
   - アクセス修飾子がない場合にフラグを立てる
```

**ルール6（型推論/var使用、42-45行目付近）:**
```
6. **型推論（var使用）**:
   - varが使用できる場所での明示的な型宣言にフラグを立てる
   - 違反例: `Dictionary<TileData, TileBase> tileMapping = new Dictionary<TileData, TileBase>();`
   - 正: `var tileMapping = new Dictionary<TileData, TileBase>();`
```

### 変更後のルール番号

| 変更前 | 変更後 | ルール名 |
|--------|--------|---------|
| 1 | 1 | エラーハンドリング方針 |
| ~~3~~ | _(削除)_ | ~~アクセス修飾子~~ |
| 4 | 2 | メソッド形式 |
| 5 | 3 | コメント規約 |
| ~~6~~ | _(削除)_ | ~~型推論（var使用）~~ |
| 7 | 4 | イベントシステム |
| 8 | 5 | LitMotion |

※ タスク3, 4 でさらにルール削除されるため、最終的なルール番号はタスク5で確定する。

## 検証

- サブエージェントのルール定義が正しく更新されていること（手動確認）
- 残留ルールの番号が連番になっていること
